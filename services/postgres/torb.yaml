version: v1.0.0
kind: service
name: postgres
inputs:
  - port
  - user
  - password
  - database
  - num_replicas
outputs:
  - port
  - user
  - password
  - database
deploy:
  helm:
    repository: https://charts.bitnami.com/bitnami
    chart: postgresql
    custom: false
  tf: